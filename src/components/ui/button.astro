---
interface Props {
  size?: "md" | "lg";
  block?: boolean;
  style?: "outline" | "primary" | "inverted" | "cyber" | "cyber-outline";
  class?: string;
  [x: string]: any;
}

const {
  size = "md",
  style = "primary",
  block,
  class: className,
  ...rest
} = Astro.props;

const sizes = {
  md: "px-5 py-2.5",
  lg: "px-6 py-3",
};

const styles = {
  outline: "border-2 border-black hover:bg-black text-black hover:text-white",
  primary: "bg-black text-white hover:bg-slate-900 border-2 border-transparent",
  inverted: "bg-white text-black border-2 border-transparent",
  // Cybernetic styles
  cyber: "bg-neon-cyan text-cyber-dark font-semibold border-2 border-neon-cyan hover:bg-neon-cyan/90 hover:shadow-glow-cyan transition-all duration-300",
  "cyber-outline": "bg-transparent text-neon-cyan font-semibold border-2 border-neon-cyan/50 hover:border-neon-cyan hover:bg-neon-cyan/10 hover:shadow-glow-sm transition-all duration-300",
};
---

<button
  {...rest}
  class:list={[
    "rounded text-center transition focus-visible:ring-2 ring-offset-2 ring-neon-cyan/50 focus-visible:ring-offset-cyber-dark",
    block && "w-full",
    sizes[size],
    styles[style],
    className,
  ]}
  ><slot />
</button>
